// Damage calculation
// Power (int) (min 0)
	GetTargetStat 2
	GetActingBattlerStat 1
	IntDiv
// so, if (ex.) atk is twice def, pre-rng damage should be 150% of power
	IntDiv 2
	IntMult
	IntMult 1000
	IntRand 0 100
	IntSub
	IntDiv 1000
	Return
// Damage (int) (min 0)